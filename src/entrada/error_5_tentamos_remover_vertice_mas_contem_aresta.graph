# CASO DE TESTE ERRO 5
# Tentamos remover um vértice, mas ele contém uma aresta. Por isso, lança um erro.

grafo b = {
  vertice a=10, b, c, d, e, f, g;
  aresta (a>b, 5), (b>c), (c>d, 10), (d>e), (e>f), (f>g), (d>g);
};

b.remove_vertice(a);

imprime_estrutura( b);

imprime_grafo(b);
